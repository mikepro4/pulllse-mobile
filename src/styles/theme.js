import { Easing } from "react-native-reanimated";

const theme = {
  background: "#000000",
  iconFill: "#ffffff",
  easing1: Easing.bezier(0.18, 0.26, 0.04, 1.06),
  easing2: Easing.bezier(0.02, 0.41, 0.42, 0.8),
};

export default theme;
